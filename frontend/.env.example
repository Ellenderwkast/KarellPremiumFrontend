# Frontend (Vite)
# URL base del backend (Express) con prefijo /api
VITE_API_URL=http://localhost:4000/api
# URL base para servir /uploads desde otro host (opcional)
# Ejemplo: VITE_UPLOADS_BASE_URL=https://api.karellpremium.com.co
